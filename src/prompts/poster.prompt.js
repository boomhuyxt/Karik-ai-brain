module.exports = {
  posterPrompt: (eventOrCampaign, targetMessage, keyVisualConcept, posterFormatSize) => 
    `Act as an Art Director & Senior Poster Designer. Create a full Poster Layout Blueprint, Color Palette, Typography rules, and Key Visual AI prompt for "${eventOrCampaign}" with message "${targetMessage}", key visual "${keyVisualConcept}", in ${posterFormatSize} format.`
};
